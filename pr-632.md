## Description
Extended the backup and recovery subsystem with automated disaster recovery governance, RTO and RPO validation, recovery training tracking, and disaster recovery documentation.

Closes #632

## Changes proposed

### What were you told to do?
Implement automated disaster recovery testing, add recovery time objective (RTO) monitoring, implement recovery point objective (RPO) validation, add disaster recovery documentation, and implement recovery team training.

### What did I do?
#### Disaster Recovery Automation
- Wired the existing `DisasterRecoveryTestingService` into the backup module and exposed an endpoint to run the comprehensive disaster recovery program.
- Added a governance service that schedules automated disaster recovery program runs and surfaces the latest report on a dashboard.
- Added objective tracking for RTO and RPO using the latest successful recovery test and the most recent recoverable backup.

#### Documentation And Training
- Added disaster recovery training tracking for named recovery roles and overdue follow-up visibility.
- Added controller endpoints for dashboard, objective review, and training status updates.
- Added `docs/disaster-recovery.md` to document testing cadence, RTO/RPO review, communications ownership, and team training expectations.

## Check List (Check all the applicable boxes)
- [x] My code follows the code style of this project.
- [x] This PR does not contain plagiarized content.
- [x] The title and description of the PR is clear and explains the approach.
- [x] I am making a pull request against the main branch (left side).
- [x] My commit messages styles matches our requested structure.
- [x] My code additions will fail neither code linting checks nor unit test.
- [x] I am only making changes to files I was requested to.

## Screenshots / Testing Evidence
- `npm run build` passes on this branch.
- `npm test -- --runInBand` still fails because of the existing `EmailService` type error in `src/auth/services/auth.service.spec.ts`, which is outside this change set.
