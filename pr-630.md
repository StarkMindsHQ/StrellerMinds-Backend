## Description
Implemented a capacity planning system that adds resource usage monitoring, capacity forecasting, autoscaling recommendations, benchmark tracking, and operational documentation.

Closes #630

## Changes proposed

### What were you told to do?
Implement capacity planning tools, add resource usage monitoring, implement auto-scaling strategies, add performance benchmarking, and implement scaling documentation.

### What did I do?
#### Capacity Planning Module
- Added a dedicated `CapacityPlanningModule` with admin endpoints for dashboard, forecasts, autoscaling guidance, snapshots, and benchmarks.
- Implemented scheduled and manual resource snapshot collection using CPU, memory, heap, uptime, and active handle telemetry.
- Added forecast logic that estimates threshold breaches and highlights operational risk levels.

#### Scaling Strategy And Documentation
- Added autoscaling guardrails with scale-out and scale-in thresholds for CPU, memory, and latency.
- Added benchmark execution and benchmark history to support release readiness and infrastructure reviews.
- Added a `docs/capacity-planning.md` runbook covering monitoring, forecasting, benchmarking, and scaling operations.

## Check List (Check all the applicable boxes)
- [x] My code follows the code style of this project.
- [x] This PR does not contain plagiarized content.
- [x] The title and description of the PR is clear and explains the approach.
- [x] I am making a pull request against the main branch (left side).
- [x] My commit messages styles matches our requested structure.
- [x] My code additions will fail neither code linting checks nor unit test.
- [x] I am only making changes to files I was requested to.

## Screenshots / Testing Evidence
- `npm run build` passes on this branch.
- `npm test -- --runInBand` still fails because of the existing `EmailService` type error in `src/auth/services/auth.service.spec.ts`, which is outside this change set.
